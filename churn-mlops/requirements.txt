# =============================================================================
# DEPENDÊNCIAS DO PROJETO — Churn Prediction MLOps
#
# Divididas em grupos para clareza e manutenção:
#   [1] Core ML       — scikit-learn e numpy para treino e inferência
#   [2] API Serving   — FastAPI + Uvicorn para o servidor HTTP
#   [3] Validação     — Pydantic (já incluído com FastAPI)
#   [4] Testes        — pytest para validação no CI/CD
#
# IMPORTANTE: versões fixadas (==) para reprodutibilidade do ambiente.
# Em produção, use pip-compile (pip-tools) ou poetry.lock para gerenciar
# atualizações de segurança de forma controlada.
# =============================================================================

# --- [1] Core ML -------------------------------------------------------
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1

# --- [2] API Serving ---------------------------------------------------
fastapi==0.111.1
uvicorn[standard]==0.30.1
httpx==0.27.0           # Cliente HTTP assíncrono (usado nos testes)

# --- [3] Serialização --------------------------------------------------
# pickle é built-in do Python — não requer instalação separada

# --- [4] Testes --------------------------------------------------------
pytest==8.2.2
pytest-asyncio==0.23.7
